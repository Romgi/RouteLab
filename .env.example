# RouteLab v1 requires no runtime environment variables or secrets.
# Keep local overrides in ignored .env files. Never expose private values through
# NEXT_PUBLIC_ variables; that prefix intentionally includes a value in the client bundle.

